# nodejs-crud

## Install

Install dependecies:
`npm i`

## Modes

- Development mode (Start with nodemon):
  `npm run start:dev`
- Production mode (Build + start):
  `npm run start:prod`
- Mode with load balancer:
  `npm run start:multi`
